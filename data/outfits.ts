export type OutfitTemplate = {
  id: string;
  title: string;
  subtitle: string;
  thumbnailUrl: string;
  imageUrl: string; // full-size image passed as nano-banana-pro image_input[1]
};

export const outfitTemplates: OutfitTemplate[] = [
  // 의상 이미지 준비 후 추가 (public/templates/outfit/ 에 이미지 저장)
];
